export default () => ({
  "webp-converter": {
    enabled: true,
    config: {
      // Using default mimeTypes by not specifying them
      options: {
        // Example options (adjust as needed)
        quality: 80,
        lossless: false,
      },
    },
  },
});
